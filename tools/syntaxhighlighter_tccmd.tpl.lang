Description="code-creation-kit template language definition file"

Keywords={
  {  Id=1,
     Regex=[[ ([MACRO_BEGIN]-[ENTRY]["Shortcut"]|[MACRO_END][MACRO_BEGIN]--[ENTRY]["Name"][BEGIN][IF][LAST_TIME][OR]|[END][MACRO_END]) ]]
  }
}

Comments = {
   {
     Block=false,
     Delimiter = { [[ # ]] }
   }
}

IgnoreCase=false
